# Learn Bootstrap

### npm install
to install all dependencies

### npm install -g lite-server
to install it on global

### lite-server
to run the app with port 6422

### npm start
to run all the scripts inside the package.json